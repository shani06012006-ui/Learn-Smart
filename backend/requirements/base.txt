Django==5.2.17
djangorestframework==3.18.1
djangorestframework-simplejwt==5.5.1
drf-spectacular==0.30.0
django-cors-headers==4.9.0
psycopg2-binary==2.9.13
python-dotenv==1.2.3

# Real-time (WebSocket presence now; chat reuses these in Module F)
channels==4.3.2
channels-redis==4.3.0
daphne==4.1.2

# Background tasks (no tasks scheduled yet in Module A, but the worker
# needs to be importable/runnable from the start per the project's tech stack)
celery==5.6.3
redis==8.1.0

Pillow==12.3.0
